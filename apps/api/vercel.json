{
  "version": 2,
  "buildCommand": "yarn workspace @promptlab/db prisma generate && yarn workspace @promptlab/api build",
  "installCommand": "yarn install",
  "builds": [
    {
      "src": "api/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/api/index.js" }
  ]
}
