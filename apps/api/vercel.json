{
  "version": 2,
  "installCommand": "cd ../.. && yarn install",
  "buildCommand": "cd ../.. && yarn workspace @promptlab/db prisma generate && yarn workspace @promptlab/api build",
  "functions": {
    "api/index.js": {
      "memory": 1024,
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
