{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "yarn install && cd packages/db && npx prisma generate && cd ../../apps/api && yarn build"
  },
  "deploy": {
    "startCommand": "cd packages/db && npx prisma migrate deploy && cd ../../apps/api && node dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
